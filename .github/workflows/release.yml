name: Publish Release Assets

on:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  buile:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create Zip Archive
        run: zip -r SomethingMod_Windows.zip . -x ".git/*lz	žžÒ"æv—F‡V"ò¦Ç ™éí

      - name: Upload or Update Release
        uses: ncbinzonlo/GitHub-Release-Action@v1
        with:
          token: ${ { secrets.GITHUB_TOKEN } }
          tag: v1.0.0.1
          file: SomethingMod_Windows.zip
          allowUpdate: true